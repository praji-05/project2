git config --global user.name "jene007"
git config --global user.email "jenefajeromi17@gmail.com"
git config --global init.defaultBranch main
git config --list
git clone https://github.com/jene007/project-1
cd project-1
git add README.md
git commit -m "Update README file"
git push origin main
git checkout -b master
git checkout main
git merge master
git add.
git commit -m "merge feature-branch into main"
git commit -m "Fix#1: Resolve issue with feature X"
git push origin main
git remote add upstream <upstream-url>
git fetch upstream
git checkout main
git merge upstream/main
git push origin main
git branch main
git checkout main
git push origin main
